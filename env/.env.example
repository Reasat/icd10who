# WHO ICD API credentials
# Register at https://icd.who.int/icdapi to obtain a client ID and secret.
# Copy this file to env/.env and fill in the values before running `make ttl`.

CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
